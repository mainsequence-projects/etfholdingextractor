# Brief

Repository-local agent support for `etfholdingextractor-161`.

Current scope:

- ETF holdings and weights extraction from supported providers
- MainSequence holdings category planning and sync from extracted holdings

Success condition:

- the repository has project-specific agent instructions
- non-scaffold skills exist for the supported CLI capabilities
- `.agents/agent_card.json` reflects the current project version and local skills
