# Tasks

- If this project gains more CLI surfaces, add matching repo-local skills under `.agents/skills/`.
- If a real agent endpoint is introduced, update `.agents/agent_card.json` with the supported interface details.
- If provider organization metadata or a stable documentation URL becomes available, replace the placeholder empty strings in `.agents/agent_card.json`.
